@model IEnumerable<CineProyecto.Models.Cliente>

@{
    ViewBag.Title = "Lista de Clientes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center my-4">Lista de Clientes</h2>

<div class="container">
    <p>
        @Html.ActionLink("Agregar Cliente", "Create", null, new { @class = "btn btn-primary mb-3" })
    </p>
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>
                    Nombre del cliente
                </th>
                <th>
                    CI Cliente
                </th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.NombreCliente)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CiCliente)
                    </td>
                    <td class="text-center">
                        @Html.ActionLink("Editar", "Edit", new { id = item.IdCliente }, new { @class = "btn btn-sm btn-warning mr-1" })
                        @Html.ActionLink("Detalles", "Details", new { id = item.IdCliente }, new { @class = "btn btn-sm btn-info mr-1" })
                        @Html.ActionLink("Eliminar", "Delete", new { id = item.IdCliente }, new { @class = "btn btn-sm btn-danger" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
